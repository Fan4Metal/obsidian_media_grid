# OS
.DS_Store
Thumbs.db

# Obsidian workspace & cache (per-machine state)
dev_vault/.obsidian/workspace*.json

# Plugin runtime data — may contain secrets (e.g. API keys)
dev_vault/.obsidian/plugins/*/data.json

# Deployed copies — sources live in the repo (./plugin, ./*.css)
dev_vault/.obsidian/plugins/media-gallery/
dev_vault/.obsidian/snippets/
