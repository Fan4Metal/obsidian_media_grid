# OS
.DS_Store
Thumbs.db

# Local test vault — not part of the repo; sources live in ./plugin.
# Holds per-machine Obsidian state, deployed plugin copies, and possibly
# secrets (e.g. plugin data.json API keys), so the whole folder is ignored.
dev_vault/
